import React from 'react';

const Header = () => {
    return (
        <div>
            <h2 className="headerColor">Expense 
                <span className="trackerColor">
                Tracker
                 </span>
            </h2>
        </div>
    );
};

export default Header;